# Task Plan: goal origin as first-class asset

## Goal
- 将“项目核心目标”定义为可 AI 生成、可用户编辑的一等资产，并作为资产原点接入现有模型。

## Steps
1. [completed] 新增核心目标资产定义文档。
2. [completed] 更新资产登记与关系，挂接到 V1 与流程资产。
3. [completed] 更新 product model 与 workflow，使目标原点进入状态流。
4. [completed] 更新 README 与 V1 文档，统一交互语义。
5. [completed] 校验文档一致性并回写计划状态。

## Validation
- `rg "goal|origin|核心目标|asset-origin" docs`
- `wc -l` for changed markdown files

## Result
- 每个 well 的核心目标已成为一等资产原点：AI 可生成，用户可编辑，且成为生成前置条件。
